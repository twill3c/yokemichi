# SPEC.md — yokemichi(避け道)

<!-- scaffold template v1.24.0 から展開(2026-09-02)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

一本指で機体を動かし、降りそそぐ弾幕を制限時間まで抜ける実時間の避けゲー。

**目玉は「解けない面を配らないこと」。**出題するすべての面について、被弾せずに
終端まで到達できる経路が実在することを、時空グリッド(位置 × tick)上の到達可能性探索で
**証明してから**配る。証明できなかった面は棄却して再生成する。
プレイヤーが死ぬのは腕のせいであって、面のせいではない。

証明した経路はゲームエンジンで再生して生存を確かめ(二経路一致)、
プレイヤーには任意で薄く重ねて見せられる —— それがこのアプリの名前である。

姉妹作は jirai-sweep(ノーゲス・マインスイーパ)と nanpure-forge(一意解保証)。
**「生成 → ソルバー検証 → 不合格なら棄却」という同じ骨格を、手番のないリアルタイムに移す。**

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 世界モデル: 座標は正規化 `[0,1] × [0,1]`。時間は tick(固定幅 `DT` 秒)。1 面は `T` tick で終わる。自機は半径 `RS` の円、弾は半径 `RB` の円 | must |
| F-02 | 自機の運動: 入力は目標点 `q`。**まず `q` を世界の矩形に押し込み**、その点へ向かって 1 tick で最大 `VMAX` だけ進む(距離が `VMAX` 以下なら到達する)。矩形は凸なので、内側の点から内側の点へ向かう限り自機は外へ出ない —— 最後にもう一度クランプする必要はない | must |
| F-03 | 弾のモデル: 弾の位置は tick の関数 `b(t) = origin + velocity·(t − t0)` であり、**自機の状態に依存しない(open-loop)**。生存区間は `[t0, t1)`(`t1` の tick には存在しない) | must |
| F-04 | 衝突判定: tick `t → t+1` のあいだ、自機と弾はそれぞれ等速直線に動くとみなす。相対運動の最近接距離を**閉形式**で求め、`RS + RB` 未満なら被弾とする(tick 間のすり抜けを起こさない)。区間 `[t, t+1]` の判定に参加する弾は `t0 ≤ t < t1` を満たすものとし、**その区間の終端位置は `t1` を越えても外挿する**。これは弾を実際よりわずかに長く危険と見なす保守側の丸めで、N-03 の片側誤りと同じ向きに倒れる | must |
| F-05 | 弾幕スクリプト: 面は「時刻付きの発射イベント列」で表す。パターン語彙は `line`(直線斉射)/ `fan`(扇状拡散)/ `ring`(円形拡散)/ `sweep`(掃射)。**イベント列は面の生成時に確定し、実行中は変化しない** | must |
| F-05a | 弾の寿命: `t1 − t0 = ⌈√2 / speed⌉ + 1`。世界の直径は `√2` なので、それだけ進んだ直線は世界の中に留まれない —— **定数ではなく世界の大きさから導く** | must |
| F-06 | 生成器: シード付き PRNG を注入。難度パラメータ + シード → 弾幕スクリプト。生成 → F-07 で検証 → 不合格なら再生成の棄却サンプリング。試行上限到達時は明示的に失敗を返す | must |
| F-06a | 非自明性: 生成した面は、**格子上のどの一点に留まり続けても生き延びられない**(定常生存点がゼロ)。判定は「その点に静止した自機」と全弾の最近接距離が `RS + RB` を下回る tick が必ず在ることによる。当初は「開始点に留まると死ぬ」だけだったが、それでは**一度隅へ動いて静止する面**が通ってしまい、証明経路が 600 tick 中 20〜32 tick しか動かなかった(loop_006 実測)—— 目玉の「避け道」が半秒で消えるので強めた | must |
| F-11a | 画面の向き: canvas の `y` は下向きなので、**世界の `y = 0` が画面の上端**である。自機は下(`y = 0.9`)から始まり、弾は上(`y < 0`)から降りてくる。単体テストは座標系に無関心なので、この取り違えは**目で見るまで緑のまま通る**(loop_006 で実際に踏んだ) | must |
| F-07 | ソルバー: 時空グリッド(`N × N` 格子 × `T+1` 層)上の到達可能性探索。格子点は `(i/(N−1), j/(N−1))`。辺 `g → g'` は (a) `\|g' − g\| ≤ VMAX` かつ (b) その区間で全弾に対し最近接距離が `RS + RB + C` 以上(クリアランス `C`)のとき通行可。開始点から `t = T` に到達できれば `solvable`。**予算(辺の評価回数の上限)付き**で、予算切れは「解けない」ではなく**「証明できなかった」**として区別する | must |
| F-07a | 格子と速度の整合: **格子間隔 `1/(N−1)` は `VMAX` より小さくなければならない。**さもないと自分自身以外の格子点へ 1 tick で届かず、遷移が自己ループだけになって証明が空になる。斜めまで届く(`√2/(N−1) ≤ VMAX`)ことを既定とし、到達可能オフセットは定数表ではなく **`WORLD` から導出する** | must |
| F-08 | 通過保証: プレイヤーに出るすべての面は F-07 で `solvable` と証明済みである。証明された経路(格子点の列)を面に添えて返す | must |
| F-09 | 再生検証: F-08 の経路を**目標点の列**としてゲームエンジン(`stepShip` と被弾判定)に入力し、`T` tick を被弾せずに走り切ることを確かめる。**結論(生存)だけでなく経路も比べる** —— 各 tick の自機位置がソルバーの格子点と一致すること(HC-065)。ソルバーは格子の上で経路を**構成**し、再生器は運動モデルを**実行**する。両者が同じ点列に着くことが、証明が実行可能であることの実地の確認である | must |
| F-10 | 難易度: **立てる面積**(各 tick で「そこに静止していれば当たらない」格子点の割合の、面全体での平均)で刻む。値が小さいほど盤面が覆われていて手ごわい。**当初案の「余白」と「隘路」は測って採らなかった**(§4「難度をどう刻んだか」)。3 段階の帯は実測で定める | must |
| F-11 | UI: ポインタ / タッチ専用。画面をドラッグすると自機が指の位置を追う。残り tick・被弾の有無・シードを表示する。**キーボードを使わずに全機能へ到達できる** | must |
| F-12 | 避け道の表示: F-08 の証明経路を薄いゴーストとして重ねて表示する。**既定は OFF**、トグルで切り替える | should |
| F-13 | 決定論: 同一シード + 同一難度 → 同一の弾幕スクリプト・同一の証明経路。シードは UI に表示し、指定して再挑戦できる | must |
| F-14 | 記録: 難度別の到達 tick 最良記録を localStorage に保存・表示する。壊れた保存値はパース失敗として空扱いにする | should |
| F-15 | 開発ハーネス: `npm run verify`(typecheck + lint + test + build)green が全ループの完了条件 | must |
| F-16 | ライセンス表示: 全ページのフッタにフリート統一書式(「MIT License © 2026 坂田哲朗 ・ GitHub ・ 歩き方 ・ 設計図 ・ App Menu」・`position: fixed; bottom: 0`・区切りの「・」は文字として置く)を表示する | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | `src/core/` は純関数のみ。**import してよいのは `src/core` の中だけ**(禁止一覧ではなく許可制 —— 知らない依存が増えたときに黙って通らないため)。`Math.random()` / `Date.now()` / `new Date()` / DOM の大域変数を**コードとして**書かない | T-090。**当初は「eslint 境界」と書いていたが、そんな規則は存在しなかった**(loop_010 で発覚・HC-077)。検査は判定規則を別モジュールに切り出し、壊し方ごとの陽性対照 9 種と、**言及を違反と誤判定しない**ことの陰性対照 5 種を持つ |
| N-02 | ソルバー・生成器の探索は必ず予算付き(ソルバー: 展開状態数の上限、生成器: 試行回数の上限)。無限ループを書かない | コードレビュー + T-xxx |
| N-03 | **ソルバーの誤りは片側だけである。**「安全」と言った面には実際に安全経路がある(健全)。逆は保証しない —— 格子に乗らない経路で抜けられる面を「危険」と誤ることはある(不完全)。この不完全性は**面を捨てすぎる方向**にしか効かないので、通過保証を壊さない | §4 の健全性論証 + T-xxx(再生検証) |
| N-04 | 生成実績ゲート: 固定シード **各難度 20 種**が試行上限内に生成成功し、全数が経路の不変量と再生検証を通り、難度の帯(F-10)に収まる。**当初は 易 100 / 並 50 / 難 20 と書いていたが、1 面あたり約 1 秒かかると分かったので 20 に下げた** —— 黙って削らず、帯の較正のほうは各難度 60 種で測ってある | T-051(validation) |
| N-05 | Next.js 静的エクスポート。ランタイム依存は react / react-dom / next のみ | T-091。**`next build` の成功では検証できない** —— 依存を足しても通るし、`output: "export"` をコメントアウトしても別の形で通りうる。`package.json` の `dependencies` の鍵集合と、`next.config.ts` の設定が**コメントアウトされずに**在ることを直接見る |
| N-06 | 面の生成(棄却サンプリングを含む)は UI をブロックしない範囲で終わる。所要時間は実測して記録する | 実測(下表)。1 面あたり 0.4〜0.9 秒。**棄却率が入る生成器ができてから判定する** |

## 4. 品質基準

### 健全性の論証(N-03 の根拠)

ソルバーが返す経路は格子点の列 `g_0, g_1, …, g_T` であり、次を満たす:

1. 各 `g_t` は連続空間 `[0,1]²` の実在の点である(格子は空間の部分集合であって、近似ではない)
2. `|g_{t+1} − g_t| ≤ VMAX` —— F-02 の運動則で、目標点を `g_{t+1}` に置けば **1 tick で正確に到達する**
3. 各区間 `[t, t+1]` で、全弾に対する最近接距離が `RS + RB + C ≥ RS + RB` —— F-04 の判定式で被弾しない

したがって「ソルバーが安全と言った経路は、ゲームエンジンでそのまま実行できて生存する」。
**判定式 F-04 をソルバーとエンジンで共有していることは循環ではない** —— 共有しなければ
両者が同じゲームについて語っていることにならない。循環を避けるために置く検査は次の 3 つ:

- **判定式そのものを別経路で検証する。** 閉形式の最近接距離を、同じ区間を 1,000 分割した
  数値サンプリングの最小値と突き合わせる(二経路一致・非循環)
- **陽性対照。** 全幅を塞ぐ壁の面を独立に構成し、ソルバーが `solvable=false` を返すことを確かめる。
  返さないなら、そのソルバーは何も証明していない
- **再生検証で経路まで比べる**(F-09)。生存という結論だけを見る照合は、別の理由で
  生き延びた実装を緑のまま通す

### ソルバーの所要時間(N-06・実測 2026-09-02)

本番設定(`gridN = 41`・`ticks = 600`・9 近傍)で 1 面を解く時間。
`solveExact`(全弾を舐める)と `solve`(空間で絞り込む)を同じ条件で測った。

| 弾の総数 | 同時最大 | 素朴版 | 高速版 | 倍率 | 弾との比較回数(素朴 → 高速) |
|---|---|---|---|---|---|
| 50 | 25 | 3.7 秒 | **0.51 秒** | 7.3 | 18,398,864 → 237,096 |
| 150 | 78 | 16.0 秒 | **0.41 秒** | 38.7 | 65,280,987 → 764,046 |
| 400 | 192 | 50.8 秒 | **0.86 秒** | 59.0 | 226,707,991 → 2,193,133 |

素朴版では**辺の本数はほとんど増えないのに時間だけ 14 倍**になる。辺ごとに
「その tick に生きている全弾」を舐めるので、費用は `辺 × 同時弾数` で決まるためである。

枝刈りの根拠は相対運動の下界

```
closestApproach(a0,a1,b0,b1) ≥ |a0 − b0| − |a1 − a0| − |b1 − b0|
```

で、`|a0 − b0| ≥ RS + RB + C + VMAX + |Δb|` の弾は最近接距離を必要余白より
小さくできない。**近似ではなく厳密**なので健全性(N-03)は変わらない ——
ただし変わらないのは**「安全か否か」の判定**だけで、最近接距離の**値**は変わる。
値を使う `validatePath` は絞り込まない全弾版を通す。

**素朴版は loop_003 の測定(2.1 / 7.5 / 25.5 秒)より遅い。**判定をオラクル越しの
呼び出しにし、比較回数を数えるようにした分である。**倍率を語るなら両版を
同じ条件で測り直さなければならない**ので、上表は今回一度に取り直した値である。

### 面の生成(F-06a を強めた後・実測 2026-09-02)

難度は「弾を減らす」ではなく「弾を遅くする」ことで易しくしている ——
**留まれる場所を無くすには盤面を覆う必要がある**ので、薄い弾幕では面が作れない。

| 難度 | 成功 | 平均試行 | 証明経路が動く tick | 経路長 | 1 面 |
|---|---|---|---|---|---|
| 易 | 6/6 | 2.2 | 82 / 600 | 2.31 | 1.1 秒 |
| 並 | 6/6 | 1.0 | 110 / 600 | 2.98 | 0.86 秒 |
| 難 | 6/6 | 1.0 | 195 / 600 | 5.31 | 0.97 秒 |

強める前は動く tick が **20〜32** しかなく、避け道が半秒で消えていた。

**試験用に縮めた設定(21×21・60 tick)では「易」が 0/6 になる。**面が短くて
遅い弾が盤面を覆いきらないためで、本番の性質ではない。**縮小した設定は本番と
同じ性質を持たない**ので、テストは「難」を使う(そこでは `解けないので棄却` も発火する)。

### 面の生成(F-06a を強める前・実測 2026-09-02)

`generateLevel` を試行上限 60 で回した結果。棄却は「解けない」と「自明(留まって抜けられる)」の 2 種。

| 設定 | 難度 | 成功 | 平均試行 | 棄却[解けず / 自明] | 1 面あたり |
|---|---|---|---|---|---|
| 本番(41×41・600 tick) | 易 | 5/5 | 2.4 | 0 / 7 | 0.92 秒 |
| 本番 | 並 | 5/5 | 1.0 | 0 / 0 | 0.41 秒 |
| 本番 | 難 | 5/5 | 1.0 | 0 / 0 | 0.49 秒 |
| 試験(21×21・60 tick) | 並 | 12/12 | 2.5 | 0 / 18 | 0.07 秒 |
| 試験 | 易 | **7/12** | 28.1 | 0 / 190 | 0.47 秒 |

**N-06 は満たす。**本番設定で 1 面あたり 0.4〜0.9 秒であり、生成中表示を出せば足りる。

**上表の「解けず 0」は、計器の読み違いである(2026-09-02 訂正)。**
`rejections.unsolvable` が数えるのは「**最初に成功するまでに外した回数**」であって
「解けない率」ではない。採用率が 96〜98% あるので最初のスクリプトがほぼ通り、
この計器はほぼ常に 0 を返す。**0 が返ったことを「機構が発火していない」と読んだのが誤り**だった
(HC-123: 計器が 0 を返した ⇒ 機構が発火していない、という含意は成り立たない)。

**試行上限を 1 にして数え直すと、出荷する難度でも経路は発火している。**

| 難度 | 標本 | 採用 | 自明で棄却 | 解けずに棄却 | 解けない率 |
|---|---|---|---|---|---|
| 易 | 200 種 | 59% | 41% | **0 件** | 0% |
| 並 | 200 種 | 98% | 2% | **1 件**(種 199) | 0.5% |
| 難 | 120 種 | 96% | 1% | **4 件**(種 20 / 68 / 101 / 114) | 3.3% |

名指しした種は T-045 で固定してある。`randomScript` を変えればこのテストは落ちる ——
**それが正しい**。生成の中身が変われば率を測り直すべきだから。

### 弾の濃さと解けなさ(実測 2026-09-02・試行上限 1・各 30 種)

難の他パラメタを据え置き、撃つ回数だけを振ったときの応答。

| 撃つ回数 | 採用 | 解けずに棄却 | 立てる面積の中央 |
|---|---|---|---|
| 55(出荷の難) | 29/30 | 1 | 0.8993 |
| 80 | 26/30 | 4 | 0.8576 |
| 110 | 24/30 | 6 | 0.8100 |
| 150 | 17/30 | 13 | 0.7532 |
| 200 | 3/30 | 27 | 0.7011 |
| 280 | 0/30 | 30 | — |

**遷移は崖ではなく、なだらかである。**「常に解ける」と「常に解けない」のあいだに
広い中間帯があり、出荷する難はその入口に居る。より手ごわい面を配りたければ、
撃つ回数を上げれば棄却率と引き換えに濃くできる(110 で 20%・150 で 43% を捨てる)。

試験設定の易が 7/12 なのは**縮小の産物**で、本番の性質ではない。面が 60 tick と短く、
易の弾幕が開始点まで届かないので大半が自明として弾かれる。
**試験用に縮めた設定は本番と同じ性質を持たない。**

### 難度をどう刻んだか(F-10・実測 2026-09-02)

**当初 F-10 が挙げた二つの指標は、測って採らなかった。**

| 当初案 | 実測 | なぜ採らないか |
|---|---|---|
| **余白**(証明経路上の最近接距離 −(`RS`+`RB`) の最小) | 易 中央 0.0063 / 並 0.0061 / 難 0.0061(下限 `C` = 0.006) | **クリアランスに張り付く。**ソルバーは `C` を超えたら改善しないので、600 tick のどこかで必ず下限に触れる。**面ではなくソルバーの閾値を測っている** |
| **隘路**(各 tick で安全な格子点の割合の**最小**) | 易 0.800–0.890 / 並 0.770–0.845 / 難 0.690–0.805(各 40 種) | 中央値の順序は正しいが、**重なりが 0.035〜0.045 あって難度を分けない** |

採ったのは**同じ系列の平均** —— 面全体でどれだけ「立っていられる」か。

| 難度 | 最小 | p10 | 中央 | p90 | 最大 |
|---|---|---|---|---|---|
| 易 | 0.9311 | 0.9363 | 0.9403 | 0.9464 | 0.9479 |
| 並 | 0.9173 | 0.9216 | 0.9242 | 0.9297 | 0.9331 |
| 難 | 0.8896 | 0.8946 | 0.8993 | 0.9047 | 0.9068 |

(各難度 60 種・本番設定)

**言えることと、言えないこと。**

- **並と難は個々の面でも分かれる** —— 分布の隙間は 0.0105。境界はその中点 **0.912**
- **易と並は個々の面では分かれない** —— 重なり幅 0.0020。**分布としてしか言えない**
  (易の p10 0.9363 > 並の p90 0.9297)
- n=25 のときは易と並も分離して見えた。**それは小標本の端点を見ていただけだった**(HC-124)

### 誤って断定しかけたこと

「隘路の最小は F-06a により恒等的に 0 になる」と、**測らずに構造から書きかけた**。誤りである。
F-06a が要求するのは「**全 tick を通して**生き延びる点が無い」ことであって、
各 tick に安全な点が無いこととは違う —— 死ぬ点が tick ごとに入れ替わればよい。
実測では最小でも 0.69 以上あった。テストが先に捕まえた(T-050b)。

### 数値の基準

- `src/core` カバレッジ: lines / functions / statements ≥ 90%、branches ≥ 85%
- 生成された面は**全数**が F-09 の再生検証に合格する(N-04 のゲートで全数検証)
- 決定論: 同一シードで生成した面が、弾幕スクリプト・証明経路ともに完全一致する
- 判定式の二経路一致: 閉形式と `K` 分割サンプリングを突き合わせる。許容差は定数で置かず、
  **解析的な上界で書く**。区間内の相対距離の二乗は `s` の二次式 `|d(s)|² = A(s−s*)² + m²`
  (`A = |w|²`、`w` は 1 tick の相対変位)なので、最小点 `s*` に最も近い格子点は
  `h/2 = 1/(2K)` 以内にあり、

  ```
  0 ≤ sampled² − closed² ≤ A · (h/2)²
  ```

  が常に成り立つ。左の不等号(閉形式は真の最小なのでサンプリングを超えない)と
  右の上界の両方を検査する。**`1e-9` という当初の見込みは撤回した(HC-073)。**

  実測(`K = 1000`・乱択 1,000 組・seed 20260902・2026-09-02):

  | 量 | 実測値 | 意味 |
  |---|---|---|
  | 差の最大 `sampled − closed` | `4.811e-5` | 当初の見込み `1e-9` は 4 桁外していた |
  | 上界に対する比の最大 | `0.9952` | **上界はほぼ達成される** = 導出が緩くない |
  | 厳密に食い違った組 | `718 / 1000` | 二つの経路は実際に別物である |
  | 最も詰まった最近接距離 | `1.074e-3` | 差が開くのは最小距離が小さい組 |

  比の最大が 1 に近いことは重要である。**届かない上界はいくらでも真になる**ので、
  「上界を超えない」だけを検査すると、実装が壊れても緑のままになりうる。
  したがってテストは上界とあわせて**比が 0.5 を超えること**も要求する(HC-079)

## 5. スコープ外

- **自機狙い弾・追尾弾・弾の相互作用。** 弾の軌道が自機の状態に依存すると、
  時空グリッドの状態が「位置 × 時刻」で閉じなくなり、F-07 の証明が成り立たない。
  F-03 の open-loop 制約はこのための前提であって、手抜きではない
- **ソルバーの完全性。** 格子外の経路まで探すことはしない(N-03)
- キーボード / ゲームパッド操作(ポインタ専用と明示する)
- 効果音・BGM
- 自機の攻撃・スコアアタック・ボス
- オンラインランキング・共有機能
- 面の非同期生成(Web Worker)。同期生成 + 生成中表示で足りる規模に収める

# TEST_SPEC.md — yokemichi(避け道)

<!-- scaffold template v1.24.0 から展開(2026-09-02) -->

## 実行規約

- `npx vitest run` を stage 3–5 の判定に使用。`npm run verify:fast`(typecheck + lint + test)を
  編集 2 回ごと、`npm run verify`(+ build)を完了条件とする
- テストは `src/**/__tests__/*.test.ts`(unit)と `tests/validation/*.test.ts`
  (validation: 生成実績ゲートなど重いもの)に置く
- 合成フィクスチャ(手組みの弾幕・手組みの盤面)は、期待値の導出前提
  (弾がその tick に実在すること・距離が意図した大小関係にあること)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)
- プロパティテストは固定シード列(`SEEDS = [1..n]`)で回す。`Math.random()` による
  シード選択は禁止(VERIF-FLAKE の予防)
- ゲートコマンドはパイプを通さず素で実行し、exit code で判定する(HC-080)

## 期待値の出所(HC-016)

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 解析解 | 導出をコメントに書く。手計算の途中式を残す |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。

## オラクルの出所

| フィクスチャ / 検算 | 出所 | 性格 |
|---|---|---|
| 最近接距離の閉形式 | 同区間を 1,000 分割した数値サンプリングの最小値 | 二経路一致(非循環)。判定式そのものを別経路で検証する。**上界が緩すぎないこと**も対で押さえる(実測 比 0.9952) |
| 証明経路の実行可能性 | ゲームエンジンでの再生(F-09) | 二経路一致。結論(生存)に加え**経路(各 tick の位置)**まで照合する(HC-065) |
| 全幅を塞ぐ壁の面 | 手組み(独立構成) | **陽性対照**。ソルバーが `solvable=false` を返さないなら何も証明していない |
| 経路を 1 tick ずらした再生 | 手組み(独立構成) | **陽性対照**。再生器が本当に被弾を見ているかを確かめる |
| 手計算の幾何(角 / 辺 / すれ違い) | 解析解 | 単体の期待値 |

## ケース一覧

`loop_001` で実装するのは **T-001 のみ**。以下は計画であり、実装したループの列に
`✓` を立てていく。

| ID | 対応要求 | ケース | 期待 | 実装 |
|---|---|---|---|---|
| T-001 | F-15 | スモーク: vitest が core の関数を import して実行できる | pass | loop_001 |
| T-005 | F-06, F-13 | PRNG(mulberry32): 同一シード / 別シード / 値域 | 同じ列 / 違う列 / `[0,1)`。**T-014 の乱択が再現することの前提** | loop_002 |
| T-010 | F-01, F-02 | 自機の運動: 距離 > VMAX / 距離 ≤ VMAX / 同一点 / 世界の外を指した場合 / 端と角の総当たり | VMAX だけ進む / 目標点に到達 / 動かない / 押し込んだ目標へ / **常に矩形の内側**(凸性) | loop_002 |
| T-011 | F-03 | 弾の位置: 生存区間の内と外 / 区間参加条件 / 終端の外挿 | 区間内は等速直線、`t1` には居ない、参加は `t0 ≤ t < t1`、終端は外挿する | loop_002 |
| T-012 | F-04 | 衝突判定(手計算): 静止どうし / すれ違い / かすめる / 最近接点が区間外 | 手計算値と一致し、`RS+RB` 未満のときだけ被弾 | loop_002 |
| T-013 | F-04 | **すり抜け**: 1 tick で自機を貫通する高速弾 | 被弾と判定する(両端の tick では離れている) | loop_002 |
| T-014 | F-04 | **二経路一致**: 閉形式 vs 1,000 分割サンプリング(乱択 1,000 組・seed 20260902) | 閉形式が真の最小 / 差が解析的上界 `A(h/2)²` 内 / **上界にほぼ触れる**(比 > 0.5)/ 実際に食い違う組が在る | loop_002 |
| T-015 | F-04 | 陽性対照: 閉形式を「両端の最小」に差し替えた実装 | T-013 の配置を見逃す(検査が効いている証拠)。貫通しない配置では一致する | loop_002 |
| T-020 | F-05 | 弾幕スクリプト: 4 パターンの展開(line / fan / ring / sweep)+ `count=1` の縮退 | 弾数・初期位置・速度・角度が手計算どおり。ゼロ除算にならない | loop_005 |
| T-021 | F-05, F-03 | スクリプトは実行中に変化しない | 二度展開しても同一。展開に自機の情報が要らない(open-loop) | loop_005 |
| T-022 | F-05a | 弾の寿命は世界の大きさから導く | 寿命ぶん直進した弾は、最も長く留まる対角線でも世界の外 | loop_005 |
| T-002 | F-07a | 格子と速度の整合 | 本番 `WORLD` で到達可能オフセットが 9 個。速度を変えると 5 個 / 1 個に変わる(**表を書き写していない**証拠) | loop_003 |
| T-030 | F-07 | ソルバー: 素通しの面(弾ゼロ) | `solvable=true`、経路長 = T+1、始点が指定の開始点 | loop_003 |
| T-030b | F-07 | 開始点の仮定(HC-075): 世界の外 / 格子点でない | どちらも例外で落ちる(黙って丸めない) | loop_003 |
| T-031 | F-07 | ソルバー: **全域が危険になる瞬間**(陽性対照) | `solvable=false` かつ `exhausted=false`(証明された不可)。前提として**格子点すべてで余白未満**を実測。間引くと解ける | loop_003 |
| T-032 | F-07 | ソルバー: 隙間が一つだけある壁(壁は世界の外まで下り切る) | `solvable=true`。**壁の線を横切る tick では必ず隙間の中に居る**。隙間を塞ぐと解けない | loop_003 |
| T-033 | F-07, N-02 | 予算: 上限 0 / 途中で尽きる | `solvable=false` かつ `exhausted=true` で即座に返る。解けた場合は予算内 | loop_003 |
| T-034 | F-07, N-03 | 経路の妥当性(不変量)+ 陽性対照 3 種 | `\|Δ\| ≤ VMAX` かつ最近接距離 ≥ `RS+RB+C`。点をずらすと `step`、居座ると `clearance`、長さ違いと世界の外もそれぞれの違反 | loop_003 |
| T-035 | N-03, N-06 | 枝刈りの下界そのもの: 乱択 20,000 組 / 等号が立つ配置 / 境界のすぐ内側 | 不等式が破れない。**等号は乱択でなく解析的に構成して示す**(HC-124)。境界の内側には実際に危険な弾が居る | loop_004 |
| T-036 | F-07, HC-065 | **二実装照合**: `solveExact` と `solve` を 5 種の面で | 結論・`exhausted`・経路・**各層の到達集合**・辺の本数がすべて一致。標本に解ける面と解けない面が両方あること | loop_004 |
| T-037 | N-06 | 枝刈りが効いていることの実測 | 同じ結論に、弾との比較回数 1/5 未満で到達する(効いていなければ照合は無情報) | loop_004 |
| T-030〜T-034 | F-07 | **両実装に同じ規格を課す**(`describe.each`) | 参照実装をテスト専用にすると、ずれても誰も気づかない(HC-065) | loop_004 |
| T-040 | F-06, F-08, F-13 | 生成器: 固定シード / 同一シード / 別シード | 面が返り経路が不変量を満たす。同一シードは完全一致、別シードは別の面 | loop_005 |
| T-041 | F-06, N-02 | 試行上限 0 | 明示的失敗(null)を返す | loop_005 |
| T-042 | F-08, F-09 | 再生検証: 証明経路をエンジンで再生(5 シード) | 生存する。かつ**各 tick の位置がソルバーの格子点と一致** | loop_005 |
| T-043 | F-06a, F-09 | 陽性対照: 開始点に留まる経路を再生 | **必ず被弾する**(非自明性ゲートが保証)。ゲートが実際に弾いた件数 > 0 も要求 | loop_005 |
| T-044 | F-08 | 生成器が「解けないので棄却」の経路を実際に通る | 極端に濃い難度で `rejections.unsolvable > 0`、かつ全滅ではない(HC-126) | loop_005 |
| T-050 | F-10 | 難易度の指標: 手組みの広い面 / 狭い面 | 余白・隘路が広い面のほうが大きい | |
| T-051 | F-08, N-04 | 生成実績ゲート(validation): 易 100 / 並 50 / 難 20 シード | 全数が上限内に生成成功し、全数が再生検証を通る | |
| T-060 | F-11 | セッション: ドラッグ入力 / 指を離す / 被弾 | `VMAX` 以内で目標を追う。`null` なら動かないが時間は進む。被弾後は凍る | loop_006 |
| T-061 | F-11 | セッション: T tick 到達 | 状態が `cleared` になり、以後進まない | loop_006 |
| T-062 | F-12 | 避け道: 切り出し / 終端 / 中身 | 現在地 + 先 N 点。はみ出さない。**返すのは証明経路そのもの**(加工しない) | loop_006 |
| T-063 | F-11, F-12, F-06a | **実ブラウザ検品**(390/768/1280 px) | 盤面が正方形・横溢れ無し・自機と弾が描かれる・避け道 ON で線の画素が**増える**・放置すると必ず被弾する。異常なら exit 1 | loop_006 |
| T-070 | F-14 | 記録: 初回 / 更新 / 非更新 / 難度別 / 壊れた保存値 | 初回は保存、良い時のみ更新、難度別に独立、パース不能は空扱い | |
| T-080 | F-16 | フッタ: フリート統一書式 | 5 項目・並び・区切りの「・」4 個・`position: fixed` を持つ | |

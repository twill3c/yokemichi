# AGENTS.md — yokemichi(避け道)エージェントハーネス

このファイルは、本リポジトリで作業するコーディングエージェントの行動規範・品質ゲート・
完了条件を定義するハーネスである。末尾の「共通規律」は scaffold-kit 管理領域であり、直接編集しない。

読む順: 本書 → SPEC.md(仕様の正本)→ TEST_SPEC.md。

## 1. プロジェクト概要

弾幕避けゲーム。core(世界モデル / 衝突判定 / 弾幕スクリプト / ソルバー / 生成器)+
Next.js 静的エクスポート UI。**出題する面は必ずソルバーで安全経路を証明する(F-08)** ——
抜けられない面は出荷されない。

- 仕様の正本は `SPEC.md`。実装とスペックが食い違う場合、スペックが正。
- スペック変更が必要な場合は、**スペック → テスト → 実装**の順(スペック駆動)。

### このプロジェクトで特に効く注意

- **弾の軌道を自機に依存させない(F-03)。** 追尾弾を足した瞬間、時空グリッドの状態が
  「位置 × 時刻」で閉じなくなり、F-07 の証明が成り立たなくなる。SPEC のスコープ外は
  好みの話ではなく、目玉の前提である
- **ソルバーとエンジンは衝突判定を共有する。** これは循環ではない(SPEC §4 の論証)。
  そのぶん、判定式そのものは別経路(数値サンプリング)で検証し、陽性対照を対で置く
- **「ソルバーが解けた」は「面が正しい」ではない。** 再生検証(F-09)は結論だけでなく
  経路まで比べる。ソルバーを直したら再生検証の陽性対照が今も撃つかを確かめる

## 2. 開発ループ(loop engineering)

すべてのタスクは末尾の **7 段階ループプロトコル**(共通規律)で進め、
`logs/loops/{loop_id}.jsonl` に `python harness/looplog.py append` で記録する。

| 段階 | yokemichi での実施 |
|---|---|
| 2 文脈読込 | SPEC.md の該当 F-xx と TEST_SPEC.md の対応 T-xxx、直近ループのログを読む |
| 3 テスト先行 | TEST_SPEC.md の該当行を実装に落とし、`npx vitest run` で赤を確認する |
| 4 実装 | 編集 2 回ごとに `npm run verify:fast`。実行のたび `test_run` を記録する |
| 5 検証 | `npm run verify`(build 込み)を green にする。ゲートコマンドはパイプを通さず素で実行する |
| 6 文書同期 | SPEC / TEST_SPEC の「実装」列を更新する。実測した数(余白・隘路・生成所要)は SPEC に書き戻す |
| 7 完了 | `looplog.py validate` 合格 + `summary` を完了報告に含める |

looplog 記録の規範(HC-001):

- 新しいイベント種別を初めて使う前に `harness/looplog.py` の EVENT_SPECS(必須フィールドと型)を確認する。推測で引数を組み立てない。
- `test_run` の passed / failed は**直前のテスト出力の数値をそのまま転記**する。記憶で書かない。

## 3. 品質ゲート(完了条件)

`npm run verify` が green であること。内訳:

| ゲート | 基準 |
|---|---|
| typecheck | `tsc --noEmit` エラー 0 |
| lint | eslint エラー 0 |
| test | 全テスト green。`src/core` カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85% |
| build | `next build`(静的エクスポート)成功 |

図・UI を触ったループでは、**実ブラウザで複数の画面幅で見る**ことを完了条件に加える(HC-041 / HC-078)。

```bash
# 1. 出荷物を作って配る
cd c:/_ClaudeCode/yokemichi && npm run build
node c:/_ClaudeCode/harness-data/serve_static.mjs c:/_ClaudeCode/yokemichi/out 4173 &

# 2. playwright を持つ姉妹プロジェクトへ検品器を複製して走らせる
#    (ESM の解決はファイルの位置で決まるので、ルートからは動かない)
cp c:/_ClaudeCode/harness-data/yokemichi_check.mjs c:/_ClaudeCode/hanshoku-atlas/
cd c:/_ClaudeCode/hanshoku-atlas && node yokemichi_check.mjs http://127.0.0.1:4173/ C:/tmp/yokemichi_shots
rm c:/_ClaudeCode/hanshoku-atlas/yokemichi_check.mjs   # 借りた側を汚さない
```

検品器は 390 / 768 / 1280 px で、盤面が正方形か・横に溢れていないか・自機と弾が
実際に描かれているか・**避け道を ON にすると線の画素が増えるか**・
**放置すると必ず被弾するか**(F-06a の実地確認)を見て、異常があれば exit 1 で落ちる。

**「避け道の線が出ている」を画素数 > 0 で見てはならない。**自機のリングが同じ差し色なので、
線が一本も描かれていなくても緑になる —— loop_006 で実際に偽の合格を出した。
必ず **OFF のときとの差**で見る。そして**撮った絵は必ず目で見る**:
上下が反転していたのも、避け道が半秒で消えていたのも、画素の計数ではなく
スクリーンショットを開いて分かった。

<!-- scaffold:block agents_core v1.24.0 -->
## 共通規律(scaffold 管理領域 — 手動編集禁止)

このセクションはスキャフォールド・レジストリが管理する。内容を変更したい場合は、
このファイルを直接編集せず、失敗ログ → HARNESS_CHANGELOG 起票 → レジストリ改訂 → `scaffoldctl update` の経路で行うこと。

### 7 段階ループプロトコル

| 段階 | 名称 | 完了条件 |
|---|---|---|
| 1 | 計画 | 対象の要求 ID を特定し、`loop_start` を記録した |
| 2 | 文脈読込 | SPEC.md / IMPLEMENTATION_GUIDE.md の該当箇所と、直近ループのログを読んだ。**この段階で扱うデータ・設定ファイルの実物を開いた**(索引・要約・記憶ではなく、本文・設定そのもの — HC-014) |
| 3 | テスト先行 | TEST_SPEC.md にトレースする失敗するテストを書き、赤を確認した。**各ケースの期待値の出所(SPEC の条項 / 実測 / 外部権威)を書いた**(HC-016) |
| 4 | 実装 | ファイル編集 2 回ごとにテストを実行し、赤のまま次の編集に進んでいない |
| 5 | 検証 | 全テスト合格 + 独立再計算(該当時)を確認した。**出荷物を実際に作った** — そのプロジェクトが公開時に通す工程(ビルド・型検査・lint 等)を一度は通し、除外設定(`.vercelignore` / `.gitignore` / `files`)を足したり変えたループでは、**削った後の木でも作れること**まで確かめた(HC-062) |
| 6 | 文書同期 | SPEC/docs と実装の乖離(SPEC-DRIFT)を解消し、生成ドキュメントを再生成した |
| 7 | 完了 | `stage_end` → `loop_end` の順に記録し(**`loop_end` はループの最終行**。後から追記しても LL-09 で拒否される — HC-013)、ループログ validate に合格し、専用コミットを積んだ |

### ループ可観測性

全ループは loop-observability の規律(LOOP_LOG_SPEC / FAILURE_TAXONOMY)に従い
`logs/loops/{loop_id}.jsonl` に記録する。失敗は気づいた瞬間に分類コード付きで記録する。
ツーストライク(LL-10)と S1 即時起票(LL-12)は本プロジェクトでも有効である。

**`looplog.py append` の出力を破棄してはならない**(`>/dev/null` 等)。記録は拒否されることがあり、
出力を捨てると「記録できていないこと」に気づけない。毎回、成否を目視で確認する(HC-013)。

**拒否されたら、その場で直すまで次へ進まない**(HC-076)。追記専用のログでは、拒否は
「あとで直せる失敗」ではない —— `loop_end` を書いてしまうと LL-09 で追記できなくなり、
監査証跡に穴が空く。`loop_end` の前に必ず二つ確かめる:

- そのループで記録するつもりだった行がすべて入っているか(とくに `commit`)
- **`failure_count` を実際に数えたか。** `correction` で無効化した行は数えない

### 外部データの採録(HC-012)

外部のコーパス・データセットから採録対象を選ぶとき:

- **メタデータからの推定で確定しない。** 題名・分類番号・ラベルは候補生成にのみ使い、
  本文・実物を見るまで `needs_review` を外さない
- **抽出器を書く前に件数オラクルを探す。** コーパス自身が持つ手がかり(題名の数詞・目次・凡例の記載・
  公表された総数)を先に探し、見つからなければ「オラクル無し」と SPEC に明記する
- **分割器・分類器は、テストを書くより先に実データ全域で被覆を測る**(HC-069)。
  「未分類 0 件」「取りこぼし 0 件」を実測してから、その値を出所としてテストを書く。
  被覆を測らずに書いたテストは、分割器の穴をそのまま期待値に固定する。
  分類できなかったものは黙って捨てず、**例外にして止める**
- **探索で書いた規則は、写さずに昇格させる**(HC-069)。探索コードと実装で正規表現・分割規則を
  二度書くと、探索時にだけ効いていた条件が静かに落ちる。写す場合は、写した先で
  **探索と同じ入力に対し同じ出力になること**を確かめる。組版・書式は目視の印象と食い違う —
  「空行で区切られている」「中央寄せされている」は、実物の全域では成り立たないことがある
- **外部形式を読むコードには「仮定が崩れたら落ちる」検算をコードの中に置く**(HC-075)。
  辞書のバイナリ・注記記法・ファイル書式は「たいていこう」で書くと、例外に当たったとき
  **黙って違う結果を出す**。仮定を書いたら同じ場所に検算を置き、通ってしまう道を作らない。
  効くのは**その場で例外にできる述語**である —— 「識別子が値を一意に決めること」
  「規則が対象ごとにばらけていないこと」「導出できるはずの値が実体と一致すること」。
  黙って通る道を残さなければ、仮定が外れた日に実装のほうが教えてくれる

### 期待値の書き方(HC-016)

テストの期待値を**観測する前に定数で書いてはならない**。書いた定数を仕様だと思い込むと、
正しい実装・正しいデータの方が落ちる(VERIF-FALSE)。

- **SPEC の保証粒度を超えない。** 超える精度を要求したくなったら、先に SPEC 側を上げる。
  例: SPEC が「暦日の一致」しか保証していないのに「分の一致」を要求してはならない
- **件数・行数は定数で書かない。** 「集合が一致する」「取りこぼしが無い」という不変量で書く。
  外部データは重複・欠落を含むので、数はデータが動くたびに壊れる
- **やむを得ず定数で書くなら、実測日と実測値をコメントに残す。** 後から見て
  「誰かの仮定」か「その日の観測」かが区別できない期待値を残さない
- **述語の射程も、書く前に観測する**(HC-040)。走査・集計・比例を主張するテストは、
  (1) 対象集合に何が入っていて何が入っていないか、(2) 主張する関係が対象の実際の性質と
  合っているか、を実物で一度確かめてから書く。とくに「全ファイルを走査する」「N に比例する」の形は、
  対象を広く取りすぎたり、模擬データの性質を実データの性質と取り違えたりしやすい。
  確かめた内容はテスト内のコメントに残す
- **既存のフィクスチャを別の検査に流用するときは、その検査が要求する性質を持つか確かめ直す**(HC-070)。
  「壊した実装を落とす」対照は、壊した箇所が実際に効く入力でなければ発火しない。
  対照のテスト本体に**その性質が成り立つことの表明**を書く
- **観測の義務は、自分で書いた例文にも及ぶ**(HC-068)。外部データだけを疑って自作物を疑わないと、
  「こう書いたつもり」と実際の性質がずれたまま期待値を書いてしまう。
  **自作物のほうが「知っているつもり」になって観測が飛ぶ。**
  期待値を書く前に、その例文が主張したい性質を実際に持つことを確かめ、
  可能なら**期待値をフィクスチャから導出**して定数で書かない
- **表示用に加工したフィールドを、検査の根拠にしない**(HC-068)。切り詰め・丸め・整形を経た値は
  見せ方の都合で動くので、実装が正しいまま表示の都合で落ちる。逆に、表示を変えたときに
  検査が黙って弱くなる方向にも壊れる。不変量は**元データ**に対して書く

### SPEC に書く文の三区分(HC-077)

SPEC の断定形は、次のどれかに分類できるように書く。分類できない文は、たいてい
**確かめていないことを断定している**。

- **実測** —— 値と実測日を添える。「10,000 文で不一致 0 件(2026-08-30)」
- **見込み** —— 「見込みである」と明示し、**確かめる手段を併記する**。
  「圧縮で 4.27 MB の見込み。本番へ向けて `--url` で測り直す」
- **実装の説明** —— 実物に当てて確かめてから書く。とくに**手段の名指し**
  (「AST で検査する」「Brotli で配られる」)は、その手段が実際に使われていることを
  確認してから書く。実装が字句走査なのに「AST で」と書けば、読んだ人はそれを信じる

見込みを断定形で書くと、外れたときに**文書のほうが証拠として振る舞う**。
外れた予測は消さず、実測と併記して残すと後続の判断材料になる。

### 品質ゲート

**「違反 0 件」「テスト全部緑」は「検査した」を意味しない。** 検出系のテストは、
対象に違反が無いときと、検査そのものが働いていないときとで、まったく同じ緑を返す。

**テストが緑であることは、出荷物が作れることを意味しない**(HC-062)。テストランナーと
出荷ビルドはモジュール解決も型の要求も異なる(vite / esbuild / tsc / webpack はどれも別物である)。
検証段でテストしか回していないと、ビルドの失敗はコミットまで、あるいは公開の直前まで見えない。
さらに、**手元でビルドが通ることと、配られる木でビルドが通ることも別である** —
アップロードされる木は除外設定で削られている。

- **禁止・検出系のテストには陽性対照を対にして置く**(HC-041)。パターンや検査器を書いたら、
  (a) 必ず捕まえるべき既知の悪い例と、(b) 撃ってはならない正当な例を同じ場所に列挙し、
  **パターン自身をテストする**。これが無い検査は、緩めたときも壊れたときも緑のままになる。
  あわせて走査対象が空でないことも別ケースで確かめる
- **例外・除外の仕掛けには、緩みすぎを止める仕掛けを対で置く。** 除外リストに挙げた対象が
  実在すること、除外マーカーが実際に必要な行にだけ付いていることを検査する。
  緩める側だけを用意すると、検査は静かに骨抜きになる
- **検査を書いたら、対象に当てる前にまず正常系へ当てて誤検出 0 を確かめる**(HC-074)。
  陰性対照を「作った例」で済ませず、**実データの正常な部分から取る**。
  とくに (a) 文字クラスや符号位置の範囲を禁止するとき (b) 外部データの構造に
  「これは○○を意味する」というラベルを与えるとき (c) 実装の内部関数をテストから直接呼ぶとき
  (実装がその関数をどう呼んでいるかを先に読む)。
  **禁止語・禁止文字の検査は「引用・言及」と「使用・依存」を分ける。**
  捨てた主張を引用して「もう使わない」と書く文、参照実装の名前を読者に告げる文、
  文字種を論じるために当の文字を引く文 —— どれも違反ではない。
  分ける線(コメント・コード柵・依存の構文形)を決め、**その線が緩みすぎないことを対照で押さえる**
- **二実装照合・二経路一致は、結論だけでなく経路も比べる**(HC-065)。最終的な数や合否が
  一致したことは、**同じ理由で一致したこと**を意味しない。片方が別の経路で偶然同じ結論に
  着いたとき、結論だけを見る照合は緑のまま何も言わない。比べられる中間量
  (呼び出し列・訪れた状態・使った資源・分岐の履歴)を少なくとも一つ含めること。
  あわせて**陽性対照**を対で置く —— 経路だけを意図的にずらした実装を用意し、
  照合がそれを落とすことを確かめる。落とせないなら、その照合は経路を見ていない。
  とくに危ないのは、**片方が教材・出荷物で、もう片方がテスト専用**のときで、ずれても誰も気づかない
- **アルゴリズムを差し替えたら、既存の不変量の根拠を再導出する**(HC-070)。「同じ出力を返すはず」で
  差し替えると、出力ではなく**保証**が変わっていることを見落とす。差し替えの際は、その実装の性質に
  依存しているテストを列挙し、根拠が残っているかを一つずつ確かめる。根拠が消えたなら、
  不変量を落とすのではなく**根拠を回復させる実装を足す**ことを先に検討する。
  小さい入力では差し替えの影響が現れないことがあるので、**実データでも掛ける**
- **段階的な課題・検査・指標を足すときは、既存のもので代替されないことを対照で示す**(HC-064)。
  (1) 期待表(どれがどこで落ちるか)を**実装前に**宣言し、新しい要素は
  **自分のための防御・条件を抜いたときだけ**落ちることを要求する。
  (2) 新しいものに既存の要素の材料を混ぜない —— 混ぜると「どれを測っているか」が言えなくなる。
  (3) 実測が宣言した表と食い違ったら、**表ではなく対象の側を直す**。
  (4) **独立性は両方向で確かめる**(HC-071)。(a) 新しいものが既存のもので**代替されない**こと。
  (b) **既存の機構が新しいものの前提を消していない**こと。(b) は
  「新しい要素が狙った状況を、実際に一度でも作れているか」を測って示す ——
  発火の記録・到達した状態・通った経路のいずれかで、**その状況が起きた証拠**を残すこと。
  証拠が残らない要素は、宣言しただけで何も測っていない。
  表を実測に合わせて書き換えたら、その対照はもう何も言わない。
  独立性が崩れているときは、たいてい**概念の切り分けが粗い**ので、そこを直すと説明もよくなる
- **オラクルが循環していないか疑う**(HC-045)。新しい検算を書いたら
  「これは主張したいことを前提にしていないか」を一度確かめる。
  検査したい当の規則を検査側でも使っていると、一致はほぼ恒等式になり、緑でも何も検査していない
- **図に添える文字(ラベル・凡例・軸・注記)は、図を生成したのと同じデータから導く**(HC-045)。
  位置も内容も座標を決め打ちしない。生成関数は骨格だけでなく、ラベルが必要とする点
  (入口・出口・極値・境界)も返す。図の側の不変量(順序・非重なり・境界)が緑でも、
  ラベルの矛盾は捕まらない
- **判定を表に出す記号(○× ・色・強調)は、仕様のどの述語に対応するかを言えること**(HC-079)。
  言えないなら記号を出さない —— **測って見せるだけの数に、良し悪しの色を付けてはならない**。
  読み手にとって記号は判定そのものであり、裏づけの無い記号は、テストが緑のまま画面だけが嘘をつく。
  **対照も同じで、対照が成り立つ前提**(比較対象が実際に違う・検出器が実際に撃つ)**を assert で固定する**。
  前提を確かめない対照は、対象が変わったときに黙って対照でなくなる
- **目で見なければ分からない性質は、テストで代替せず検品の手順として残す**(HC-041)。
  図が読めるか・絵が嘘でないかは自動化しにくい。図を触ったループでは
  実ブラウザ(または実際の出力)で確認する、を完了条件に含める。
  **その検品の道具自体が壊れていないか**も疑う — 取得に失敗した画面を撮っても
  「撮影しました」と出る。道具は失敗を検出したら終了コードで知らせるようにする
- **検品器は対象の実装ではなく振る舞いで書く**(HC-080)。要素名・要素の同一性・
  出力の経路に依存させない。同じものを別の要素で描くことがある(数えるなら名前ではなく
  子の総数で)、UI は再描画で要素を作り直す(掴み置きせず毎回引き直す)、
  終了コードはパイプの先で別物にすり替わる(`| tail` の後の `$?` は tail のもの)。
  実装に乗った検品器は、**偽の異常・偽の正常・停止**のいずれかを起こす
- **検品器にも陽性対照を置く**(HC-080)。「異常なし」を返したとき、その検品器が
  実際に異常を捕まえられることを一度確かめる。そして
  **大量に落ちたとき・意味不明に固まったときは、対象ではなく検品器を先に疑う**
- **見るのは一つの幅ではない**(HC-078)。図・表に手を入れたループの完了条件に、
  **複数の画面幅で実際に見る**を入れる。列が潰れて一文字ずつ折り返す、図が細長い柱になる —
  どちらも「要素が在るか」「本文が出ているか」の検査は緑のまま通す。
  あわせて機械で捕まえられる代理指標を置く: **横の溢れ**と**縦の伸びすぎ**。
  縦は列の潰れがよく出る(1 ページが 16,000px を超えたら、まず表を疑う)。
  代理指標は目視の代わりではなく、**目視を忘れたときの網**である
- **オラクルの閾値を置く前に、二つの実装が理論上一致しうるかを確かめる**(HC-073)。
  非決定性(同点・タイブレーク・浮動小数の丸め・走査順・並列の到着順)が結果に入りうるなら、
  「完全一致」は原理的に達成できない。閾値は**一致する量**と
  **一致させるために揃える規則**に分けて書く。分けずに置くと、達成不能な閾値を掲げたまま
  実装を疑い続けることになる。**揃える規則を相手の実装を見て決めたなら、
  別の標本で外挿を確かめる** —— 合わせた標本で検証しても何も分からない

### この環境で書くとき

- **日本語の本文に別字種・制御文字が紛れる**(HC-072)。字形が近いキリル文字・ハングルは
  読んでも気づけず、制御文字は**構文エラーにならない壊れ方**をする。
  `python harness/text_hygiene.py` を、文章やソースをまとめて触ったループの検証段で走らせる。
  記述例など意図して置く場合は、その行に `text-hygiene:allow` を書く。
  **区切り文字に生の制御バイトを使わない** —— `\u0000` のようにエスケープで書く
- **Bash 呼び出しをまたいで cwd は持ち越される**(HC-038)。記録・検証など
  「プロジェクト直下で走ること」が前提のコマンドは、毎回 `cd <絶対パス> && ...` を前置する。
  相対パスで起動するスクリプトは、どのプロジェクトの配下で解決されたかを出力に含める
- **破壊的な git 操作は、対象リポジトリを引数で明示する**(HC-038)。`git -C <絶対パス> ...`
  の形で書き、cwd に依存させない。ブランチ名の変更・reset・force push・履歴改変は、
  直前に `git -C <パス> rev-parse --show-toplevel` で対象を出力して目視してから実行する
- **Git Bash はスラッシュで始まる引数を Windows パスに変換する**(HC-038)。
  ルートパス・URL パスを外部コマンドに渡すときは `MSYS_NO_PATHCONV=1` を前置する。
  渡した引数が化けていないことは、受け側で引数をそのまま出力して確かめる
- **エスケープ・正規表現・改行を含む編集は、シェル経由の言語内文字列で行わない**(HC-042)。
  ファイル編集ツールで直接書く。やむを得ずスクリプトを通すときは、(a) 置換対象が
  見つからなかったら例外で落とす、(b) 置換後の該当行を必ず目視する、
  (c) 制御文字が混入していないかを確認する。**構文エラーにならない壊れ方をする**ことを
  前提に手順を組むこと — 単語境界のエスケープが制御文字に潰れると、
  ソースを開いても検索しても見た目に何も無いまま、検査が無言で死ぬ

### エスカレーション規範

以下の場合は作業を止め、`escalation` を記録してから人間に確認する:
仕様の複数解釈(SPEC-AMB 相当)/ スコープ外ファイルへの変更が必要になった /
破壊的操作(履歴改変・データ削除・強制 push)/ 同種の修正の 3 回目の失敗(PROC-LOOP)。

### コミット規約

Conventional Commits(feat/fix/test/docs/refactor/chore)。スキャフォールド更新は
`chore: scaffold vX.Y.Z` の専用コミットで行い、機能変更と混ぜない。
<!-- /scaffold:block agents_core -->
